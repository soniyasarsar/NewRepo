public with sharing class Result {
    public static Integer calculator(String ', Integer x, Integer Y)
    {
    	Integer add;
    	add= x+y;
      	system.debug(add);
        return add;
    
    } 
}